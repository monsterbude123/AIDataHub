# DAG任务编排页 - 页面设计规范

> 页面路径: `/project/[id]/scheduler/dag`
> 所属项目: 数据项目管理模块
> 覆盖 MASTER 规范: 全屏画布设计，需要最大化可用空间

## 页面目标

可视化编排项目内 DAG 任务，配置任务依赖关系，管理项目级调度流程。

## 布局结构

```
┌─────────────────────────────────────────────────────────┐
│ 面包屑: 数据项目 > 项目列表 > [项目名称] > 任务调度 > DAG编排 │
│ 项目信息: 当前阶段 [执行阶段]  进度 65%                   │
│ 操作栏: [保存] [验证] [运行] [全屏]                       │
├──────────┬──────────────────────────────────────────────┤
│ 节点面板  │              DAG画布                         │
│ (节点类型)│                                              │
│          │  拖拽节点，连线配置依赖                        │
│          │                                              │
│          │                                              │
├──────────┼──────────────────────────────────────────────┤
│ 属性面板  │  选中节点显示属性配置                         │
└──────────┴──────────────────────────────────────────────┘
```

## 项目信息头部

在 DAG 编排页顶部显示项目上下文：

- 项目名称
- 当前阶段（标签显示）
- 项目进度（进度条）

**目的**: 让用户在编排 DAG 时清楚当前项目状态，确保任务编排与项目阶段匹配。

## 左侧节点面板

可拖拽节点类型（按项目关联模块分类）:

| 分类     | 节点类型                   | 关联模块     |
| -------- | -------------------------- | ------------ |
| 数据集成 | 数据接入、数据探查、标准化 | 数据集成模块 |
| 数据治理 | 质量检测、标签提取         | 数据治理模块 |
| 数据处理 | Spark SQL、Python脚本      | 数据处理服务 |
| 控制流   | 条件分支、循环             | -            |

**节点面板样式**:

- 宽度: 240px，可折叠到 48px
- 拖拽节点到画布添加节点
- 悬浮显示节点类型说明

## 中间DAG画布

- 使用 React-Flow 渲染
- 支持拖拽移动节点
- 支持拖拽节点边缘连接两个节点 (上游 → 下游)
- 支持点击节点选中，右侧显示属性
- 支持右键菜单操作
- 支持画布缩放平移
- 箭头表示依赖关系：上游完成 → 下游开始

### 节点显示

每个节点显示：

- 节点名称
- 节点类型图标
- 关联任务名称
- 当前状态（如果正在执行）

**节点样式**:

- 宽度: 160px
- 高度: 60px
- 圆角: 8px
- 状态色边框

### 连线功能

- **创建连线**: 从节点右侧拖拽连接点到另一节点左侧
- **删除连线**: 右键点击连线，选择"删除"选项
- **连线样式**: 灰色线条（#94A3B8），带箭头指示方向
- **禁止自连**: 节点不能连接自己
- **禁止重复**: 相同两个节点之间只能有一条连线

### 右键菜单

| 目标 | 菜单选项 | 说明                         |
| ---- | -------- | ---------------------------- |
| 节点 | 编辑     | 打开右侧属性面板编辑节点配置 |
| 节点 | 查看任务 | 跳转到关联任务的详情页       |
| 节点 | 删除     | 删除节点及其所有相关连线     |
| 连线 | 删除     | 仅删除该连线，保留两端节点   |

**右键菜单样式**:

- 菜单宽度: 150px
- 菜单项高度: 36px
- 菜单背景: 白色，带阴影
- 菜单项hover: 浅灰色背景（#F1F5F9）
- 删除选项: 红色文字（#EF4444）表示危险操作

## 右侧属性面板

选中节点后显示配置:

### 基本属性

| 属性     | 类型     | 说明                                      |
| -------- | -------- | ----------------------------------------- |
| 节点名称 | Input    | 可编辑，最大50字符                        |
| 任务类型 | Select   | 数据接入/探查/标准化/质量检测/Spark SQL等 |
| 关联任务 | Select   | 从项目关联任务中选择                      |
| 执行参数 | TextArea | JSON配置                                  |
| 超时设置 | Input    | 分钟，默认60                              |
| 重试次数 | Input    | 失败重试次数，默认3                       |
| 失败策略 | Select   | 继续/停止整个流程                         |
| 负责人   | Select   | 从项目成员中选择                          |

### 资源配置引用

> 节点执行时使用的基础设施配置

```
┌─────────────────────────────────────────────────────────────┐
│ 资源配置                                                      │
│                                                             │
│ 配置来源                                                      │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ ○ 继承DAG默认配置                                      │   │
│ │ ● 节点独立配置                                         │   │
│ └───────────────────────────────────────────────────────┘   │
│                                                            │
│ 执行引擎 (根据任务类型)                                        │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ 引擎类型: [Spark ▼]                                    │   │
│ │ 引擎配置: [spark-project-default ▼]                    │   │
│ │                                                       │   │
│ │ 配置预览:                                              │   │
│ │ • spark.executor.memory: 8g                          │   │
│ │ • spark.executor.cores: 4                            │   │
│ │ [查看完整配置]                                         │   │
│ └───────────────────────────────────────────────────────┘   │
│                                                            │
│ 资源队列                                                      │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ 队列选择: [继承DAG配置 ▼]                               │   │
│ │ ○ 继承DAG配置                                          │   │
│ │ ○ 指定队列: [queue-tenant-a ▼]                         │   │
│ │                                                       │   │
│ │ 节点优先级: [中 ▼] (覆盖DAG优先级)                      │   │
│ └───────────────────────────────────────────────────────┘   │
│                                                            │
│ Worker节点 (可选)                                             │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ [✓] 指定Worker执行                                    │   │
│ │ Worker组: [worker-group-spark ▼]                       │   │
│ │ 选择策略: [负载最低 ▼]                                  │   │
│ └───────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

| 属性         | 类型   | 说明                           |
| ------------ | ------ | ------------------------------ |
| 配置来源     | Radio  | 继承DAG配置/节点独立配置       |
| 引擎类型     | Select | 根据任务类型自动匹配或手动选择 |
| 引擎配置引用 | Select | 引用引擎配置ID                 |
| 队列选择     | Select | 继承DAG配置或指定队列          |
| 节点优先级   | Select | 可覆盖DAG级优先级              |
| Worker组     | Select | 指定执行Worker组               |

**属性面板样式**:

- 宽度: 360px
- 可折叠收起
- 资源配置区域可展开/收起

## DAG级别调度配置

底部配置栏:

### 基本信息

| 配置项     | 类型        | 说明                       |
| ---------- | ----------- | -------------------------- |
| DAG名称    | Input       | 当前DAG名称                |
| 调度策略   | Select      | 定时调度/事件触发/依赖触发 |
| Cron表达式 | Input       | 定时调度时配置             |
| 告警配置   | MultiSelect | 超时/失败/数据异常告警     |

### 默认资源配置

> DAG下所有节点的默认资源配置，节点可覆盖

```
┌─────────────────────────────────────────────────────────────┐
│ 默认资源配置                                                  │
│                                                             │
│ 默认执行引擎                                                  │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ Spark引擎: [spark-project-default ▼]                   │   │
│ │ Flink引擎: [flink-project-default ▼]                   │   │
│ │ DataX引擎: [datax-project-default ▼]                   │   │
│ │                                                       │   │
│ │ 说明: 节点根据任务类型自动选择对应引擎                    │   │
│ └───────────────────────────────────────────────────────┘   │
│                                                            │
│ 默认资源队列                                                  │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ 队列选择: [queue-project-a ▼]                           │   │
│ │                                                       │   │
│ │ 队列信息:                                              │   │
│ │ • 类型: YARN                                          │   │
│ │ • 可用资源: 30GB内存 / 20 vCore                       │   │
│ │ • 当前使用: 40%                                       │   │
│ │                                                       │   │
│ │ DAG优先级: [中 ▼]                                      │   │
│ └───────────────────────────────────────────────────────┘   │
│                                                            │
│ 默认Worker配置 (可选)                                         │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ [✓] 配置默认Worker组                                   │   │
│ │ Worker组: [worker-group-project ▼]                     │   │
│ │ 选择策略: [负载最低优先 ▼]                              │   │
│ └───────────────────────────────────────────────────────┘   │
│                                                            │
│ 资源限制                                                      │
│ ┌───────────────────────────────────────────────────────┐   │
│ │ 最大并行任务数: [5]                                     │   │
│ │ 单任务最大内存: [16] GB                                 │   │
│ │ DAG总内存限制: [64] GB                                 │   │
│ └───────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

| 配置项         | 类型   | 说明                   |
| -------------- | ------ | ---------------------- |
| Spark引擎      | Select | 默认Spark引擎配置引用  |
| Flink引擎      | Select | 默认Flink引擎配置引用  |
| DataX引擎      | Select | 默认DataX引擎配置引用  |
| 队列选择       | Select | DAG默认执行队列        |
| DAG优先级      | Select | 高/中/低               |
| Worker组       | Select | 默认Worker组           |
| 最大并行任务数 | Input  | 同时执行的任务数量上限 |

### 配置继承说明

```
项目级资源配置
    ↓ 继承
DAG默认资源配置
    ↓ 继承/覆盖
节点级资源配置
```

- DAG配置继承项目级默认配置
- 节点可继承DAG配置或独立配置
- 节点独立配置可覆盖DAG默认值

## 操作按钮

| 按钮     | 功能          | 说明                           |
| -------- | ------------- | ------------------------------ |
| 保存     | 保存DAG配置   | 必须验证通过才能保存           |
| 验证     | 验证DAG正确性 | 检查环路、孤立节点、参数完整性 |
| 立即运行 | 手动触发执行  | 启动一次DAG执行                |
| 全屏     | 全屏画布      | 扩展画布区域                   |

## 验证规则

前端验证必须通过才能保存:

1. **不能有环路**: 检测循环依赖
2. **不能有孤立节点**: 每个节点必须有入口或出口
3. **参数完整性**: 所有必需参数已配置
4. **任务有效性**: 所有节点关联有效的项目任务
5. **负责人设置**: 每个节点必须设置负责人（从项目成员中选择）

## 项目级特殊设计

### 任务关联约束

- DAG节点只能关联**项目内已关联的任务**
- 如果节点类型需要的任务未在项目中关联，需先到任务管理页关联

### 阶段匹配提示

- DAG编排时显示当前项目阶段
- 如果编排的任务超出当前阶段范围，给出提示

### 权限继承

- DAG保存/删除权限继承项目角色
- 仅项目经理和成员可编辑 DAG
- 只读者仅可查看 DAG 结构

## 交互规范

- **Ctrl+Z**: 撤销操作
- **Ctrl+Shift+Z**: 重做操作
- **Delete**: 删除选中节点
- **空格 + 拖拽**: 平移画布
- **滚轮**: 缩放画布
- **双击节点**: 编辑名称
- **右键节点**: 显示菜单
- **右键连线**: 显示删除菜单
- **左键空白**: 关闭菜单和属性面板

## 性能考虑

- 画布超过50个节点提示简化视图
- 使用虚拟滚动节点面板
- 防抖处理画布变更保存

## 权限控制

| 操作    | 项目经理 | 成员 | 只读者 |
| ------- | -------- | ---- | ------ |
| 创建DAG | ✓        | ✓    | ✗      |
| 编辑DAG | ✓        | ✓    | ✗      |
| 删除DAG | ✓        | ✗    | ✗      |
| 运行DAG | ✓        | ✓    | ✗      |
| 查看DAG | ✓        | ✓    | ✓      |

## 与基础设施模块的关联

### 配置引用关系

DAG执行时引用基础设施配置：

| 关联项   | 引用方式       | 说明                         |
| -------- | -------------- | ---------------------------- |
| 引擎配置 | 引用引擎配置ID | 节点执行时使用的计算引擎参数 |
| 资源队列 | 引用队列ID     | 任务提交到的YARN/K8s队列     |
| Worker组 | 引用Worker组ID | 任务执行的Worker节点组       |
| 配置模板 | 可选引用       | DAG预设配置模板              |

### 执行流程

```
DAG触发执行
    ↓
读取DAG默认资源配置
    ↓
遍历节点，读取节点配置
    ↓
合并配置 (节点覆盖DAG默认)
    ↓
从基础设施模块获取完整配置
    ↓
提交任务到队列
    ↓
分配Worker执行
```

### 配置引用示例

```json
{
  "dagId": "dag-001",
  "name": "daily-etl-flow",
  "defaultConfig": {
    "engineSpark": "spark-project-default",
    "engineDataX": "datax-project-default",
    "queue": "queue-project-a",
    "workerGroup": "worker-group-project",
    "priority": "medium"
  },
  "nodes": [
    {
      "nodeId": "node-1",
      "type": "spark-sql",
      "config": {
        "inherit": true, // 继承DAG默认配置
        "engineOverride": "spark-high-mem" // 可选覆盖
      }
    },
    {
      "nodeId": "node-2",
      "type": "datax-migration",
      "config": {
        "inherit": true
      }
    }
  ]
}
```

### 资源分配机制

DAG执行时的资源分配：

1. **队列资源**: DAG整体使用队列资源配额
2. **并行限制**: 最大并行任务数控制并发
3. **内存限制**: 单任务和总内存限制防超额
4. **优先级调度**: 高优先级任务优先执行

## 移动端适配

### 移动端限制

DAG编排涉及复杂的拖拽、连线操作，移动端采用只读模式：

| 功能         | 桌面端 | 移动端       |
| ------------ | ------ | ------------ |
| 查看DAG结构  | ✓      | ✓            |
| 拖拽节点     | ✓      | ✗            |
| 创建连线     | ✓      | ✗            |
| 编辑节点属性 | ✓      | ✗            |
| 保存/运行DAG | ✓      | ✗            |
| 查看节点详情 | ✓      | ✓ (底部抽屉) |

### 移动端布局

```
┌─────────────────────────────┐
│ DAG名称: daily-etl-flow     │
│ 状态: 运行中  节点: 5       │
├─────────────────────────────┤
│                             │
│   DAG静态预览图             │
│   (点击节点查看详情)        │
│                             │
├─────────────────────────────┤
│ 节点列表                    │
│ ┌─────────────────────────┐ │
│ │ 1. 数据接入     ✓ 完成  │ │
│ │ 2. 数据探查     ● 运行中│ │
│ │ 3. 质量检测     ○ 待执行│ │
│ │ 4. 数据同步     ○ 待执行│ │
│ │ 5. 结果通知     ○ 待执行│ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

### 移动端操作

- **点击节点**: 底部弹出节点详情抽屉
- **查看任务**: 跳转关联任务详情页
- **查看执行日志**: 跳转日志中心

### 移动端提示

移动端显示提示：

> "DAG编排功能请使用桌面端访问，当前仅支持查看。"

## 可访问性设计

### 键盘操作规范

DAG 编排器支持完整的键盘操作：

#### 全局快捷键

| 按键               | 功能                    |
| ------------------ | ----------------------- |
| `Ctrl + S`         | 保存 DAG                |
| `Ctrl + Z`         | 撤销操作                |
| `Ctrl + Shift + Z` | 重做操作                |
| `Ctrl + F`         | 聚焦到搜索框            |
| `Delete`           | 删除选中节点            |
| `Escape`           | 取消选中 / 关闭属性面板 |
| `?`                | 显示快捷键帮助面板      |

#### 画布导航

| 按键             | 功能                               |
| ---------------- | ---------------------------------- |
| `Tab`            | 在节点面板、画布、属性面板之间切换 |
| `↑` `↓` `←` `→`  | 在节点之间移动焦点 (按拓扑顺序)    |
| `Enter`          | 选中节点，打开属性面板             |
| `Space + 方向键` | 平移画布                           |
| `+` / `-`        | 放大/缩小画布                      |
| `0`              | 重置视图                           |

#### 节点操作

| 按键               | 功能             |
| ------------------ | ---------------- |
| `Enter` (未选中时) | 选中节点         |
| `Enter` (已选中时) | 打开属性面板编辑 |
| `Delete`           | 删除选中节点     |
| `Ctrl + C`         | 复制选中节点     |
| `Ctrl + V`         | 粘贴节点         |
| `Ctrl + D`         | 复制并放置节点   |

#### 连线操作

| 按键                | 功能         |
| ------------------- | ------------ |
| `L` (选中节点后)    | 创建连线模式 |
| `方向键` (连线模式) | 选择目标节点 |
| `Enter` (连线模式)  | 确认连线     |
| `Escape` (连线模式) | 取消连线     |

**焦点样式**:

- 焦点节点: 2px 虚线边框, Primary色 (#2563EB)
- 焦点连线: 2px 实线边框, 高亮色
- 焦点顺序: 工具栏 → 节点面板 → 画布节点 → 属性面板

### 屏幕阅读器支持

#### DAG 画布 ARIA 标签

```html
<div
  role="application"
  aria-label="DAG编排画布"
  aria-describedby="dag-instructions"
>
  <span id="dag-instructions" class="sr-only">
    使用方向键在节点之间导航，Enter 选中节点，Delete 删除节点。 按 ?
    查看完整快捷键列表。
  </span>
</div>
```

#### 节点 ARIA 标签

```html
<div
  role="button"
  aria-label="节点: 数据接入, 类型: Spark SQL, 状态: 待执行, 上游节点 0 个, 下游节点 2 个"
  aria-describedby="node-actions"
>
  <span id="node-actions" class="sr-only">
    按 Enter 编辑节点，按 Delete 删除
  </span>
</div>
```

#### 连线 ARIA 标签

```html
<div
  role="separator"
  aria-label="连线: 从 数据接入 到 数据探查"
  aria-describedby="edge-actions"
>
  <span id="edge-actions" class="sr-only"> 按 Delete 删除连线 </span>
</div>
```

### 文字列表视图

为无法使用图形界面的用户提供文字列表视图：

```
┌─────────────────────────────────────────────────────────────┐
│ DAG结构列表视图  [切换到图形视图]                             │
├─────────────────────────────────────────────────────────────┤
│ DAG名称: daily-etl-flow                                      │
│ 状态: 编辑中  节点数: 5  连线数: 4                           │
├─────────────────────────────────────────────────────────────┤
│ 节点列表 (按执行顺序)                                        │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 序号 │ 节点名称  │ 类型      │ 状态   │ 上游  │ 下游  │ 操作     │ │
│ │ 1    │ 数据接入  │ 数据源    │ 待执行 │ -     │ 2     │ [编辑]   │ │
│ │ 2    │ 数据探查  │ 探查任务  │ 待执行 │ 1     │ 1     │ [编辑]   │ │
│ │ 3    │ 质量检测  │ 质量规则  │ 待执行 │ 2     │ 1     │ [编辑]   │ │
│ │ 4    │ 数据同步  │ DataX     │ 待执行 │ 3     │ 1     │ [编辑]   │ │
│ │ 5    │ 结果通知  │ 消息推送  │ 待执行 │ 4     │ -     │ [编辑]   │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 连线列表                                                     │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 序号 │ 源节点    │ 目标节点  │ 操作                     │ │
│ │ 1    │ 数据接入  │ 数据探查  │ [删除]                   │ │
│ │ 2    │ 数据探查  │ 质量检测  │ [删除]                   │ │
│ │ 3    │ 质量检测  │ 数据同步  │ [删除]                   │ │
│ │ 4    │ 数据同步  │ 结果通知  │ [删除]                   │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ [添加节点] [添加连线] [保存DAG] [验证DAG]                    │
└─────────────────────────────────────────────────────────────┘
```

**列表视图特性**:

- 按 Tab 键在表格行之间导航
- 按 Enter 键编辑节点
- 屏幕阅读器可正确读取所有信息
- 支持添加节点、添加连线操作

### 颜色备用指示

| 信息类型 | 颜色指示 | 备用指示              |
| -------- | -------- | --------------------- |
| 节点类型 | 不同颜色 | 节点图标 + 类型标签   |
| 节点状态 | 状态色   | 状态图标 + 文字标签   |
| 连线方向 | 箭头     | 方向文字 "从 X 到 Y"  |
| 验证错误 | 红色边框 | ⚠ 图标 + 错误提示文字 |
| 选中状态 | 高亮边框 | "已选中" 文字标签     |

**节点状态图标**:
| 状态 | 图标 | 文字 |
|------|------|------|
| 待执行 | ○ | "待执行" |
| 运行中 | ● (旋转动画) | "运行中" |
| 完成 | ✓ | "完成" |
| 失败 | ✗ | "失败" |
| 已禁用 | ⊘ | "已禁用" |

### 高对比度模式

| 元素     | 普通模式      | 高对比度模式 |
| -------- | ------------- | ------------ |
| 节点边框 | 1px, #E2E8F0  | 2px, #000000 |
| 选中节点 | Primary色边框 | 3px, #000000 |
| 连线     | 1px, #94A3B8  | 2px, #000000 |
| 箭头     | 填充色        | 黑色填充     |
| 网格背景 | 浅灰色        | 关闭网格     |

### 减少动画模式

```css
@media (prefers-reduced-motion: reduce) {
  .dag-node {
    transition: none;
  }
  .dag-edge {
    animation: none;
  }
  .dag-canvas {
    /* 禁用画布缩放动画 */
    transition: none;
  }
}
```

### 验证反馈

验证结果需要提供文字形式的反馈，不仅依赖颜色：

**验证成功**:

```
✓ 验证通过: DAG 结构正确
  - 无循环依赖
  - 无孤立节点
  - 所有节点参数完整
```

**验证失败**:

```
✗ 验证失败: 发现 2 个问题
  1. [错误] 节点 "数据探查" 存在循环依赖
  2. [警告] 节点 "结果通知" 缺少负责人设置
```

### 可访问性检查清单

- [ ] 所有节点可通过键盘选中、编辑、删除
- [ ] 连线可通过键盘创建和删除
- [ ] 焦点顺序符合逻辑 (按拓扑顺序)
- [ ] 焦点状态清晰可见 (高对比度边框)
- [ ] 节点和连线有完整 ARIA 标签
- [ ] 提供文字列表视图作为图形替代
- [ ] 颜色信息有备用指示 (图标/文字)
- [ ] 支持高对比度模式
- [ ] 支持减少动画偏好
- [ ] 验证结果提供文字反馈
- [ ] 屏幕阅读器可正确读取所有操作和状态
