{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && grep -lE '(sk_|api_key|secret|password|token|private_key|BEGIN RSA PRIVATE KEY)' \"$CLAUDE_TARGET_FILE\" 2>/dev/null && exit 2 || exit 0",
            "timeout": 10
          }
        ]
      }
    ]
  }
}