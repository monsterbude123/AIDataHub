# Database Configuration
# Database file path (default: :memory: for in-memory database)
DB_PATH=data/system-auth.db

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-change-in-production

# Server Configuration
PORT=3000
NODE_ENV=development